/** @type {import('next').NextConfig} */
const nextConfig = {}
// In your next.config.js
module.exports = {
    reactStrictMode: true,
    noHydration: true, // Disable client-side hydration
  }
  

module.exports = nextConfig
